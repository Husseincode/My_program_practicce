#include <stdio.h>

/**
 * main - calculates three variables
 * Return: 0 (success)
 */

int main(void)
{
	int x, y, z;

	x = 12;

	y = 13;

	z = 15;

	int total = x + y + z;

	printf("The sum is %d", total);

	return (total);
}
